<% provide(:title, 'New Customer') %>
	<div class = "form">
	<h1> Create new customer </h1>
	<%= form_for(@customer) do |f| %>
		<%= render 'fields', f:f %>
		<%= f.submit 'Create customer', class: 'btn' %>
	<% end %>
</div>