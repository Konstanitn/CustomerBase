<% provide(:title, 'Edit Customer') %>
	<div class = "form">
	<h1> Edit customer </h1>
	<%= form_for(@customer) do |f| %>
		<%= render 'fields', f:f %>
		<%= f.submit 'Edit customer', class: 'btn' %>
	<% end %>
</div>